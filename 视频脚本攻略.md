# 视频脚本

用PyCharm创建video-script-gengrator项目时默认使用虚拟环境（给的项目提供隔离和独立的开发环境，给不同项目使用不同版本的解释器和库，更好维护项目之间独立的依赖库，坏处是每次都要解释器和库，占用空间大）



```
requirements.txt
用来储存项目所需要的依赖库以及精准版本号
在终端运行pip install -r requirements.txt即可安装
重要的库：streamlit、langchain、langchain-community、langchain-core、langchain-openai、wikipedia

如何生成的requirements.txt：终端输入pip freeze>requirements.txt
```

utlis.py：所有和ai大模型交互的代码

## 项目作用

这是一段 **LangChain + OpenAI 大模型 + 维基百科检索** 的短视频脚本自动生成代码

输入：视频主题、时长、创意度、OpenAI 密钥

输出：维基百科检索素材、短视频标题、完整结构化视频脚本

~~~python
#生成符合对话大模型格式（system/human/ai）的提示词模板，导入ChatPromptTemplate
from langchain.prompts import ChatPromptTemplate
from langchain_openai import ChatOpenAI
from langchain_community.utilities import WikipediaAPIWrapper

# import os
#通过调用generate_script函数，获得视频内容和标题
#参数subject视频主题，video_length视频时长，api_key视频脚本创作性，api_key（api密钥）
#generate_script函数完成获得视频标题，调用维基百科API获得信息，获得视频脚本内容

def generate_script(subject, video_length, creativity, api_key):
    #获得视频标题， ChatPromptTemplate有from_messages的方法接受消息列表作为参数，支持角色：system、human、ai
    title_template = ChatPromptTemplate.from_messages(
        [
            ("human", "请为'{subject}'这个主题的视频想一个吸引人的标题")  # human：模拟用户发给AI的一段话
        ]
    )
    #获得视频脚本内容
    script_template = ChatPromptTemplate.from_messages(
        [
            ("human",
             """你是一位短视频频道的博主。根据以下标题和相关信息，为短视频频道写一个视频脚本。
             视频标题：{title}，视频时长：{duration}分钟，生成的脚本的长度尽量遵循视频时长的要求。
             要求开头抓住限球，中间提供干货内容，结尾有惊喜，脚本格式也请按照【开头、中间，结尾】分隔。
             整体内容的表达方式要尽量轻松有趣，吸引年轻人。
             脚本内容可以结合以下维基百科搜索出的信息，但仅作为参考，只结合相关的即可，对不相关的进行忽略：
             ```{wikipedia_search}```""")
        ]
    )
#定义模型
    model = ChatOpenAI(openai_api_key=api_key, temperature=creativity)

    title_chain = title_template | model
    script_chain = script_template | model

    title = title_chain.invoke({"subject": subject}).content

    search = WikipediaAPIWrapper(lang="zh")
    search_result = search.run(subject)

    script = script_chain.invoke({"title": title, "duration": video_length,
                                  "wikipedia_search": search_result}).content

    return search_result, title, script

# print(generate_script("sora模型", 1, 0.7, os.getenv("OPENAI_API_KEY")))
~~~

```
search_result：获得维基百科搜索结果
title：得到视频标题
script：得到脚本
```

前端代码运行：streamlit run main.py

~~~python
import streamlit as st
from utills import generate_script  #使用generate_scri获得AI回应，既不用导入Langchain相关的库

st.title("🎬 视频脚本生成器")

with st.sidebar:
    openai_api_key = st.text_input("请输入OpenAI API密钥：", type="password")
    st.markdown("[获取OpenAI API密钥](https://platform.openai.com/account/api-keys)")

subject = st.text_input("💡 请输入视频的主题")
video_length = st.number_input("⏱️ 请输入视频的大致时长（单位：分钟）", min_value=0.1, step=0.1)
creativity = st.slider("✨ 请输入视频脚本的创造力（数字小说明更严谨，数字大说明更多样）", min_value=0.0,
                       max_value=1.0, value=0.2, step=0.1)
submit = st.button("生成脚本")

if submit and not openai_api_key:
    st.info("请输入你的OpenAI API密钥")
    st.stop()
if submit and not subject:
    st.info("请输入视频的主题")
    st.stop()
if submit and not video_length >= 0.1:
    st.info("视频长度需要大于或等于0.1")
    st.stop()
if submit:
    with st.spinner("AI正在思考中，请稍等..."):
        search_result, title, script = generate_script(subject, video_length, creativity, openai_api_key)
    st.success("视频脚本已生成！")
    st.subheader("🔥 标题：")
    st.write(title)
    st.subheader("📝 视频脚本：")
    st.write(script)
    with st.expander("维基百科搜索结果 👀"):
        st.info(search_result)

~~~



代码上传到github上：

1.终端输入pip freeze > requirements.txt，生成的requirements.txt里面有所有的库

